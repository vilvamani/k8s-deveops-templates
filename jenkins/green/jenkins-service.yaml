---
apiVersion: v1
kind: Service
metadata:
  name: jenkins-green
  labels:
    app: jenkins-green
spec:
  type: NodePort
  ports:
    - name: ui
      port: 8080
      targetPort: 8080
      nodePort: 30002
      protocol: TCP
    - name: slave
      port: 50000
      protocol: TCP
  selector:
    app: jenkins-green

